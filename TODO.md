# LLMflare 开发路线

本文档记录 `v1.0.0` 之后的工程改进项。优先级按安全和错误消费风险排序；完成项保留，便于追踪版本演进。

## P0 · 消费保护

- [x] 不再把 Mistral 账户可见模型全部标记为免费；只有目录提供可靠零价格证据或维护了明确清单时才进入免费筛选。
- [x] 为客户端密钥增加每分钟请求数、过去 24 小时请求数、本月 Token 和单次最大输出 Token 限制。
- [x] 支持立即停用或重新启用客户端密钥。
- [ ] 增加 Provider 级预算和告警；只有获得可靠价格数据时才计算金额，不能用模型可见性推断免费。

## P1 · 鉴权与密钥安全

- [ ] 将客户端鉴权从 KV 整体数组迁移到 D1 或 Durable Object，避免吊销延迟与并发覆盖。
- [ ] 为动态 Provider Key 引入 Secrets Store 或信封加密；继续要求使用低额度、可撤销的专用 Key。
- [ ] 为管理接口提供可选的 Cloudflare Access 校验，并保留 `ADMIN_TOKEN` 作为本地部署方案。

## P1 · 协议正确性

- [ ] 补齐非流式 Responses 与 Messages 的工具调用、结束原因和 usage 字段转换。
- [ ] 增加真实 Provider 的受控端到端兼容测试，覆盖 Chat、Responses、Messages、流式与函数工具。
- [ ] 对未知或不支持的协议字段返回明确错误，避免静默丢弃关键能力。

## P1 · 请求安全

- [ ] 对无 `Content-Length` 的 JSON 请求实施流式字节限制，并把超限响应规范为 HTTP 413。
- [ ] 增加请求字段运行时校验，限制自定义 Provider 类型、URL 协议和异常参数范围。

## P2 · 网关可靠性

- [ ] 增加统一首字节和完整请求超时，不再只对 NVIDIA Messages 设置特殊超时。
- [ ] 增加可配置重试、熔断、Provider 健康状态和同模型故障转移。
- [ ] 区分存活检查与依赖就绪检查，让 `/health` 能反映关键绑定状态。

## P2 · 开源交付

- [ ] 让干净克隆自动生成 Worker 类型并通过 `npm run check`。
- [ ] 添加 CI、许可证、版本变更记录和正式 Release 流程。
- [ ] 固定开发依赖更新策略，避免长期依赖 `latest`。
- [ ] 定期核对 README 与管理台能力，避免文档声称尚未实现的功能。
